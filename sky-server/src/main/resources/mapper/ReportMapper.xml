<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ReportMapper">

    <select id="queryTurnover" resultType="java.math.BigDecimal">
        select sum(order_price) from order_table
        where order_time between #{dateList[0]} and #{dateList[dateList.size() - 1]}
    </select>
</mapper>
